---
# «Упомянул СУБД — значит, PostgreSQL». Фильтр смотрит не на запрет,
# а на выбор, поэтому unless ему не нужен: отказ разрешённое называет сам.
domain: [субд, база данных, бд, mongodb, mysql, sqlite, redis, clickhouse, оракл, oracle]
allow: [postgresql, postgres]
---

База данных только PostgreSQL. Другие СУБД, включая MongoDB, MySQL
и SQLite, использовать нельзя — ни как основное хранилище, ни рядом
с основным.
